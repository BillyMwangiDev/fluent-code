[package]
name = "fluent-resource-monitor"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sysinfo = "0.39"

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
strip = true
